# Скрипты для работы с проектом

## create-github-tasks.mjs

Скрипт для автоматического создания задач в GitHub проекте на основе `docs/ROADMAP.md`.

### Требования

1. **GitHub Personal Access Token** с правами:
   - `repo` - для создания issues
   - `project` - для добавления issues в проект

   Создайте токен на: https://github.com/settings/tokens

2. **Node.js** версии 18 или выше

### Использование

```bash
# Базовое использование (проект #2 пользователя vanyukov)
GITHUB_TOKEN=your_token_here node scripts/create-github-tasks.mjs

# С указанием параметров
GITHUB_TOKEN=your_token_here \
PROJECT_NUMBER=2 \
OWNER=vanyukov \
REPO=kv.FinPal \
node scripts/create-github-tasks.mjs
```

### Переменные окружения

- `GITHUB_TOKEN` (обязательно) - Personal Access Token
- `PROJECT_NUMBER` (опционально, по умолчанию: `2`) - номер проекта
- `OWNER` (опционально, по умолчанию: `vanyukov`) - владелец репозитория
- `REPO` (опционально, по умолчанию: `kv.FinPal`) - название репозитория

### Что делает скрипт

1. Парсит `docs/ROADMAP.md` и извлекает:
   - Все милстоуны (M0-M5)
   - Задачи каждого милстоуна
   - Подзадачи и описания
   - Критерии завершения

2. Создает issues в репозитории для каждой задачи:
   - Заголовок: `[Milestone X (MX)] Название задачи`
   - Тело: включает описание, подзадачи, цель милстоуна
   - Лейблы: автоматически добавляется лейбл с названием милстоуна

3. Добавляет созданные issues в указанный GitHub проект

### Пример вывода

```
📋 Парсинг ROADMAP.md...
✅ Найдено милстоунов: 6

🔍 Получение ID проекта...
✅ ID проекта: 12345678

📦 Обработка Milestone 0 (M0): Подготовка и архитектура...
  ➕ Создание задачи: Архитектура проекта...
  ✅ Создана: #1 - [Milestone 0 (M0)] Архитектура проекта
  🔗 Добавление в проект...
  ✅ Добавлена в проект
  ...

✨ Готово! Создано задач: 29
```

### Примечания

- Скрипт добавляет задержку 500ms между запросами, чтобы не превысить rate limit GitHub API
- Issues добавляются в первую колонку проекта (обычно "To do" или "Todo")
- Если колонка "To do" не найдена, используется первая доступная колонка
- **Важно:** Для Projects API v2 может потребоваться использование GraphQL API. Если автоматическое добавление в проект не работает, добавьте issues вручную через веб-интерфейс GitHub

### Troubleshooting

**Ошибка: "Failed to get projects"**
- Убедитесь, что токен имеет права `project` (для GraphQL API)
- Проверьте, что проект существует и доступен
- Для Projects v2 нужен токен с правами на чтение проектов

**Ошибка: "Project #X not found"**
- Убедитесь, что указан правильный номер проекта
- Для user projects номер можно найти в URL: `https://github.com/users/USERNAME/projects/NUMBER`
- Проверьте, что проект существует и доступен вашему токену

**Ошибка GitHub CLI: "authentication token is missing required scopes [read:project]"**
- Обновите права токена GitHub CLI:
  ```bash
  gh auth refresh -s read:project
  ```
- Или используйте веб-интерфейс для добавления issues в проект

**Issues создаются, но не добавляются в проект**
- Это нормально, если проект недоступен через API
- Добавьте issues вручную через веб-интерфейс:
  1. Откройте проект: `https://github.com/users/vanyukov/projects/2`
  2. Нажмите "+" в нужной колонке
  3. Выберите "Add item" и найдите созданные issues
- Или используйте GitHub CLI (после обновления прав):
  ```bash
  gh auth refresh -s read:project
  gh project item-add 2 --owner vanyukov --url <issue-url>
  ```

